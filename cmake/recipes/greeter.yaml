root: True
inherit: [cmake]

depends:
   - name: cmake
     use: [tools]
     forward: True
   - libgreet

checkoutSCM:
    scm: import
    url: src/greeter/

buildScript: |
   cmakeBuildAll

packageScript: |
   cmakePackageAll
